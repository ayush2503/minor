const userDetails={
    userDetails:{},
    userloader:false,
}

export const userReducer=(state=userDetails,action)=>{
    switch(action.type)
    {   
        default :
            return state;

    }
}